name: release

# Build the listing image and push it to GHCR when the package or dockerfile
# changes on main. The gateway box pulls this image to deploy as a tinsnip
# quadlet (see the private tinsnip repo, hosts/gateway/listing.container).
on:
  push:
    branches: [main]
    # Only rebuild when something that affects the IMAGE changes -- NOT on
    # docs/tests/experiments pushes, which would otherwise force a needless
    # GHCR build + auto-update swap on the box. To force a build after a
    # build-config change, use "Run workflow" (workflow_dispatch) or bump the
    # package version.
    paths:
      - 'src/**'
      - 'pyproject.toml'
      - 'dockerfile'
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  image:
    name: build & push image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write          # push to ghcr.io/<owner>/listing
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        # cross-arch emulation so the amd64 runner can build the arm64 image
        # the Oracle box (Ampere/aarch64) needs.
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        # container driver: required for the gha build cache export below
        uses: docker/setup-buildx-action@v3

      - name: Commit subject
        # Carried as an image label so the gateway's auto-update notifier can
        # say WHAT a deploy shipped without any network call from the box.
        id: subject
        run: echo "subject=$(git log -1 --pretty=%s)" >> "$GITHUB_OUTPUT"

      - name: Image metadata (tags + labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=long
            type=semver,pattern={{version}}
          labels: |
            com.dynamicalsystem.commit-subject=${{ steps.subject.outputs.subject }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./dockerfile          # listing's Dockerfile is lower-case
          # arm64 for the Oracle Ampere box; amd64 kept for dev/CI parity.
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
